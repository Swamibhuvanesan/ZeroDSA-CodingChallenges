def sum_of_natural_numbers(N):
    # Using the formula to calculate the sum
    return (N * (N + 1)) // 2

# Example usage:
N1 = 5
N2 = 6

print(f"Input: N={N1} -> Output: {sum_of_natural_numbers(N1)}")
print(f"Input: N={N2} -> Output: {sum_of_natural_numbers(N2)}")
